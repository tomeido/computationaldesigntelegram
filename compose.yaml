services:
  bot:
    build: .
    env_file: .env
    volumes:
      - ./config:/app/config:ro
      - bot-data:/app/data
    restart: unless-stopped
    init: true
    mem_limit: 1536m
    cpus: 2.0

volumes:
  bot-data:
